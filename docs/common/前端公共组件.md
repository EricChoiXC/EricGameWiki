# 公共组件

## 一. 地址本（Address）

1. 提供组织架构的选择

2. 要求实现弹窗，包含两个页签：地址本页签和查询页面，效果示意如下：

```
地址本页签：
┌------------------------------┐
| 地址本 | 查询 |               |
├------------------------------┤
| 根节点          | 选项1       | 
|  L 组织架构xxx  | 选项2       |
|  L xxxxx       | xxxxxx      |
|                | （无分页列表）|
|                |             |
├------------------------------┤
| 已选项：xxx, yyy ...          |
└------------------------------┘
查询页签：
┌------------------------------┐
| 地址本 | 查询 |               |
├------------------------------┤
|  [(搜索文本框)       ] (查询) |
├------------------------------┤
| 选项1 (组织架构层级)          |
| 选项2 (组织架构层级)          |
| xxxxxx                       |
| 分页列表                     |
├------------------------------┤
| 已选项：xxx, yyy ...          |
└------------------------------┘
```
3. 地址本页签左侧为组织架构树，根据此处的选中项，在右侧列出属于该组织架构下的组织架构，在右侧进行选中

4. 查询页面上侧为查询条件，单个字段可同时匹配名称、编号、简称、拼音简称 字段，必须输入内容才允许查询，否则返回空；中部返回查询结果，在中部进行选中；

5. 两个页签共用选中的数据

6. 组件绑定参数如下：

   | 参数名     | 说明                                                                                                                |
      | ---------- |-------------------------------------------------------------------------------------------------------------------|
   | fieldId    | 关联的id值字段，必填                                                                                                       |
   | fieldName  | 关联的名称值字段，必填                                                                                                       |
   | orgType    | 可选的组织架构类型，默认'ALL'<br />ALL - 全部<br />ORG - 机构<br />DEPT - 部门<br />ORGDEPT - 机构或部门<br />PERSON - 人员<br />POST - 岗位 |
   | multi      | 是否多选，默认 `false`                                                                                                   |
   | septarator | 多选的分隔符，默认';'，要求仅限符号                                                                                               |
   | limitRange | 限制的组织架构范围，默认为空，填入组织架构的 `fieldId` 值数组，限制范围体现在组织架构树的根节点，和列表查询默认条件上                                                  |
   | callback   | 点击确认选择后的回调函数                                                                                                      |

## 二. 附件组件（Attachment)

1. 提供附件的上传、下载等功能

2. 组件绑定参数如下：

   | 参数名         | 说明                                                         |
      | -------------- | ------------------------------------------------------------ |
   | fieldModelId   | 传递给接口的`fieldModelId`字段值                             |
   | fieldModelName | 传递给接口的`fieldModelName`字段值，必填                     |
   | fieldKey       | 传递给接口的`fieldKey`字段值                                 |
   | multi          | 是否可上传多个附件，默认`false`                              |
   | fileType       | 可上传的文件后缀类型，默认为空（不限制）                     |
   | uploadCallback | 上传结束后的回调函数                                         |
   | uploadValidate | 确认上传文件到触发后端上传接口中间的校验函数，函数不为空时要求函数返回必须是 `true` 才能继续上传 |
   | deleteCallback | 删除结束后的回调函数                                         |

## 三. 选择器组件

### 普通选择器组件（Simple-Selector）

1. 提供对数据库来源数据的选择

2. 组件绑定参数如下：

   | 参数名     | 说明                                                         |
      | ---------- | ------------------------------------------------------------ |
   | fieldId    | 关联的id值字段，必填                                         |
   | fieldName  | 关联的名称值字段，必填                                       |
   | url        | 请求的列表查询接口url，必填                                  |
   | filter     | 可筛选项，对象数组，对象示例如下：<br />`{"text" : "查询的中文名", "name" : "查询的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | show       | 列表显示项，对象数组，对象示例如下：<br />`{"text" : "显示的中文名", "name" : "显示的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | multi      | 是否多选，默认 `false`                                       |
   | septarator | 多选的分隔符，默认';'，要求仅限符号                          |
   | callback   | 确认选择后的回调函数                                         |

### 树形选择器组件（Tree-Selector)

1. 提供对树形结构的数据的选择

2. 绑定参数如下：

   | 参数名     | 说明                                                         |
         | ---------- | ------------------------------------------------------------ |
   | fieldId    | 关联的id值字段，必填                                         |
   | fieldName  | 关联的名称值字段，必填                                       |
   | parentKey | 父级字段的字段名，必填 |
   | url        | 请求的列表查询接口url，必填                                  |
   | filter     | 可筛选项，对象数组，对象示例如下：<br />`{"text" : "查询的中文名", "name" : "查询的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | show       | 列表显示项，对象数组，对象示例如下：<br />`{"text" : "显示的中文名", "name" : "显示的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | multi      | 是否多选，默认 `false`                                       |
   | septarator | 多选的分隔符，默认';'，要求仅限符号                          |
   | callback   | 确认选择后的回调函数                                         |

### 分类选择器组件（Category-Selector)

1. 基于从分类进行查询的数据选择

2. 绑定参数如下：

   | 参数名         | 说明                                                                                                                    |
            |-------------|-----------------------------------------------------------------------------------------------------------------------|
   | fieldId     | 关联的id值字段，必填                                                                                                           |
   | fieldName   | 关联的名称值字段，必填                                                                                                           |
   | categoryKey | 分类字段的字段名，必填                                                                                                           |
   | categoryUrl | 分类查询接口url，必填 |
   | categoryShow | 分类显示项，对象数组，对象示例如下：<br />`{"text" : "显示的中文名", "name" : "显示的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | url         | 请求的列表查询接口url，必填                                                                                                       |
   | filter      | 可筛选项，对象数组，对象示例如下：<br />`{"text" : "查询的中文名", "name" : "查询的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]`  |
   | show        | 列表显示项，对象数组，对象示例如下：<br />`{"text" : "显示的中文名", "name" : "显示的字段名称"}`<br />默认值： `[{"text" : "名称", "name" : "fieldName"}]` |
   | multi       | 是否多选，默认 `false`                                                                                                       |
   | septarator  | 多选的分隔符，默认';'，要求仅限符号                                                                                                   |
   | callback    | 确认选择后的回调函数                                                                                                            |

## 四. 日期时间选择器

提供对日期、时间等数据要求的选择，包含多个不同类型的选择器

### 单日期时间选择器

1. 可从日期、时间、日期时间进行选择，包含对特定场景进行选择

2. 绑定参数如下

   | 参数名         | 说明                                                                                                                 |
      | -------------- |--------------------------------------------------------------------------------------------------------------------|
   | fieldName      | 选择器关联的字段，必填                                                                                                        |
   | calendarType   | 选择器类型，默认 `datetime`<br />year - 年<br />month - 年月<br />week - 周<br />date - 日期<br />time - 时间<br />datetime - 日期时间 |
   | dateFormat     | 日期格式，默认 `yyyy-MM-dd`，可按 `calendarType` 约束为 `yyyy`、`yyyy-MM`、`yyyy-MM-dd` 等                                         |
   | timeFormat     | 时间格式，默认 `HH:mm:ss`，可按 `calendarType` 约束为 `HH`、`HH:mm`、`HH:mm:ss` 等                                                 |
   | datetimeFormat | 日期时间格式，默认 `yyyy-MM-dd HH:mm:ss`，可按业务需要配置为只到时或时分                                                                    |
   | yearRange      | 可选年份范围或年份列表，如 `2024-2030`、`[2024,2025,2026]`、`2024-`、`-2030`                                                       |
   | monthRange     | 可选月份范围或月份列表，如 `1-12`、`[1,3,5,7,9,11]`                                                                              |
   | hourRange      | 可选小时范围或小时列表，如 `6-18`、`[8,9,10,14,15,16]`                                                                           |
   | minuteRange    | 可选分钟范围或分钟列表，如 `0-59`、`[0,15,30,45]`                                                                                |
   | weekDays       | 可选星期范围或星期列表，如 `[1]`、`[1,2,3,4,5]`，约定 `1` 为星期一，`7` 为星期日                                                             |

### 范围日期选择器

1. 可从日期、时间、日期时间进行选择，包含对特定场景进行选择

2. 绑定参数如下（参数约定与单日期时间选择器保持一致）：

   | 参数名         | 说明                                                                                                                 |
      | -------------- |--------------------------------------------------------------------------------------------------------------------|
   | fieldName      | 选择器关联的字段，必填                                                                                                        |
   | calendarType   | 选择器类型，默认 `datetimerange`<br />daterange - 日期范围<br />datetimerange - 日期时间范围<br />monthrange - 年月范围 |
   | dateFormat     | 日期格式，默认 `yyyy-MM-dd`，可按 `calendarType` 约束为 `yyyy`、`yyyy-MM`、`yyyy-MM-dd` 等                                         |
   | timeFormat     | 时间格式，默认 `HH:mm:ss`，可按 `calendarType` 约束为 `HH`、`HH:mm`、`HH:mm:ss` 等                                                 |
   | datetimeFormat | 日期时间格式，默认 `yyyy-MM-dd HH:mm:ss`，可按业务需要配置为只到时或时分                                                                    |
   | yearRange      | 可选年份范围或年份列表，如 `2024-2030`、`[2024,2025,2026]`、`2024-`、`-2030`                                                       |
   | monthRange     | 可选月份范围或月份列表，如 `1-12`、`[1,3,5,7,9,11]`                                                                              |
   | hourRange      | 可选小时范围或小时列表，如 `6-18`、`[8,9,10,14,15,16]`                                                                           |
   | minuteRange    | 可选分钟范围或分钟列表，如 `0-59`、`[0,15,30,45]`                                                                                |
   | weekDays       | 可选星期范围或星期列表，如 `[1]`、`[1,2,3,4,5]`，约定 `1` 为星期一，`7` 为星期日                                                             |

