<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiki.admin.sys.org.model.mapper.OrgUserLoginLogMapper">

    <resultMap id="logMap" type="com.wiki.admin.sys.org.model.dto.OrgUserLoginLogDo">
        <id column="field_id" property="fieldId"/>
        <result column="field_user_id" property="fieldUserId"/>
        <result column="field_login_time" property="fieldLoginTime"/>
        <result column="field_login_ip" property="fieldLoginIp"/>
        <result column="field_login_success" property="fieldLoginSuccess"/>
        <result column="field_message" property="fieldMessage"/>
    </resultMap>

    <sql id="cols">field_id, field_user_id, field_login_time, field_login_ip, field_login_success, field_message</sql>

    <select id="selectByUserId" resultMap="logMap">
        SELECT <include refid="cols"/> FROM admin_org_user_login_log
        WHERE field_user_id = #{fieldUserId}
        ORDER BY field_login_time DESC
        <if test="needPage"> LIMIT #{pageSize} OFFSET #{offset} </if>
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1) FROM admin_org_user_login_log WHERE field_user_id = #{fieldUserId}
    </select>

    <select id="selectByCondition" resultMap="logMap">
        SELECT l.field_id, l.field_user_id, l.field_login_time, l.field_login_ip, l.field_login_success, l.field_message
        FROM admin_org_user_login_log l
        <if test="whereSql != null and whereSql != ''"> WHERE ${whereSql} </if>
        <choose>
            <when test="orderBySql != null and orderBySql != ''"> ${orderBySql} </when>
            <otherwise> ORDER BY l.field_login_time DESC </otherwise>
        </choose>
        <if test="needPage"> LIMIT #{pageSize} OFFSET #{offset} </if>
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(1) FROM admin_org_user_login_log l
        <if test="whereSql != null and whereSql != ''"> WHERE ${whereSql} </if>
    </select>

    <select id="countConsecutiveFails" resultType="long">
        SELECT COUNT(1) FROM admin_org_user_login_log
        WHERE field_user_id = #{fieldUserId}
          AND field_login_success = 0
          AND field_login_time &gt; COALESCE(
              (SELECT MAX(field_login_time) FROM admin_org_user_login_log
               WHERE field_user_id = #{fieldUserId} AND field_login_success = 1),
              '1970-01-01 00:00:00'
          )
    </select>

    <insert id="insert">
        INSERT INTO admin_org_user_login_log
        (field_id, field_user_id, field_login_time, field_login_ip, field_login_success, field_message)
        VALUES
        (#{fieldId}, #{fieldUserId}, #{fieldLoginTime}, #{fieldLoginIp}, #{fieldLoginSuccess}, #{fieldMessage})
    </insert>

</mapper>
